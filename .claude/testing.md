# Testing

Frontend JS is unit-tested by the backend vitest runner — both the pure calc modules and glue (`load.js` abort-supersede, `api.js` fetch, renderer copy). `backend/src/frontend-*.test.ts` import `../../frontend/js/*.js` (kept out of `dist`/`tsc` by the `*.test.ts` exclude in `backend/tsconfig.json`). From `backend/`: `npx vitest run` (full) or `npx vitest run frontend-insights-calc` (substring path filter). `npm run test:coverage` enforces the coverage floor in `backend/vitest.config.ts` (v8 over `src/**/*.ts` and `frontend/js/**/*.js`; the frontend sits outside vitest's root, so it needs `allowExternal` *and* an absolute include path — v8 matches `include` against absolute filenames, and a relative `../frontend/js/**` silently matches nothing). Thresholds are a ratchet set to the measured numbers: raise them when coverage grows, never lower them to pass. Forgejo has no CI, so run it before deploying test-affecting work. Renderers (`hero.js`, `heatmap.js`, `estimator.js`, `insights.js`, `chart.js`, `tabs.js`) take an optional `{ document }` deps bag (estimator and insights also `{ nowMs }`; chart also `{ nowMs, Chart, matchMedia }`) so copy tests can call them with `test-support/fake-dom.ts` — no jsdom.

## Coverage

DST 92/100-slot days, deadline next-day rollover (and a same-day 23:59 deadline with tomorrow loaded bounding at tomorrow's first slot, incl. a 23h day), `parseDeadlineMinutes` (integer minutes, empty → null), past-window filtering (a slot whose exclusive end equals now is dropped), the unclamped window-end boundary, `slotSpanLabel` colon-form spans, the hero's cheap-rank bands, the insight cards' per-tab now-index (including the `30 min` / `1h 15min` / exact-hour countdown branches), chart ghost-series wall-clock alignment (not index zip), load abort-supersede (aborted rejection does not `showError`, superseded resolve does not overwrite state — for day data and for a late-resolving superseded heatmap), isolated renderer throws, cold heatmap failure `showHeatmapError` vs warm failure keeping the cached grid (`hasCachedHeatmap`), heatmap `showHeatmapError` vs empty-data placeholder (Finnish copy pinned on the renderer, not just the callback), the heatmap success path (`vko N` caption, Monday-first row labels taken from `row.day` rather than the row index, 0–23 hour headers, per-cell tooltip text and pointer offset, no listeners on greyed cells — `fakeEl` records listeners and exposes `dispatch`), day-data fetch failure still starts the heatmap (and a heatmap throw on that path still calls `showHeatmapError`), last-known-good on refresh failure (hero `refreshFailed` / stale `klo` notes), estimator unpublished-tomorrow vs no-window copy plus the success Halvin/Kallein/Säästö cards (colon spans, euro toFixed(2)) and empty-input/no-slot placeholders, tomorrow-tab midnight fallback, an enabled Huomenna click re-renders insights+chart, hero cheap-rank caption/tint/yesterday/empty-state, insight renderer writes all six fields including placeholders, `priceToColor` endpoints (including a 0 cell on a negative range), zero/negative `collectPrices` and heatmap NUMERIC cells (0 is a real price, not missing), empty-week heatmap min/max = 0, and deadline-string / slot-assembly helpers. `frontend-api.test.ts` covers the fetch layer (abort signal on every request, 404 → null, yesterday and tomorrow degrading to null, caller-abort of either still rejecting, today/now network failures still rejecting so last-known-good is kept, `fetchHeatmap` hitting `/porssi/api/prices/heatmap`) against the shared fetch stubs. `eurToCents` is pinned next to `formatCents` (numeric chart axis vs display string). `findSlotContaining` is called with `HOUR_MS` (hourly 'Nyt' marker) as well as `SLOT_MS`. `frontend-ema.test.ts` pins `emaAggregate` bucketing by absolute hour: 23/25 buckets on DST days, a missing `:00` slot still starts its own hour dated at the hour start rather than merging into the previous one (also driven through hourly `renderChart`), and both fall-back 03:xx hours stay apart when the second 03:00 is missing (finding #9929). `frontend-chart.test.ts` pins that `renderChart` actually wires those helpers: empty primary paints `Ei hintatietoja saatavilla` without constructing Chart; today+hourly at :20 places the `Nyt` annotation on bucket 0 (HOUR_MS — SLOT_MS would miss); today+15min at :20 places it on slot 1 (SLOT_MS — HOUR_MS would keep hour-start); tomorrow tab omits the marker; ghost datasets equal `alignSecondaryByWallClock` (nulls on spring-forward hour 3), not zip-by-index; a re-render calls the previous instance's `destroy()` before constructing the next; bar mode is `type: 'bar'` with the ghost kept `type: 'line'`; labels reach Chart in colon form (two `03:00` on the fall-back hourly day). `frontend-chart-series.test.ts` pins `chartSeries` without a Chart constructor (25/23 hourly buckets on fall-back/spring-forward, now index by absolute instant in the repeated hour, null on empty primary); `frontend-chart-config.test.ts` pins `buildChartConfig` bar/area branches, tick stride (every 12th slot / 3rd hour), tooltip em dash, reduced motion, and fresh option objects per call. Chart ghost alignment includes 15-min fall-back vs fall-back (in-order consume of each duplicated HH:MM) and 25h vs 24h (reuse last value per wall-clock key), not only `{ hourly: true }`. `frontend-api.test.ts` and `collectors/sahkotin-fetch.test.ts` spy `AbortSignal.timeout` and pin `FETCH_TIMEOUT_MS` (15s), matching `spot-hinta-fetch.test.ts`. `frontend-main.test.ts` pins `init()`'s production `createLoader` deps (`hasCachedData` / `hasCachedHeatmap` / `noteStale` / `tomorrowTab` → `renderTomorrowTab`, 60s `createSlotRefresh`) and that the tab, chart-type and resolution toggles all call the injected `renderChart` (the chart toggles without re-rendering insights), and that `init()` boots the estimator once (finding #9927). `frontend-tabs.test.ts` pins `renderTomorrowTab` through its `{ document }` seam (disable + title, fallback to Tänään, re-enable). `frontend-csp.test.ts` pins the no-inline-boot HTML, `main.js` auto-init (skipped under Vitest), and `deploy/nginx-porssi.conf` CSP (finding #7954) plus its `/porssi/api/` `proxy_pass` to `127.0.0.1:3600/api/` (finding #9954). `frontend-ui.test.ts` pins `wireToggleGroup` ignoring disabled clicks (checked at click time, not wire time) and `setActive` (exact predicate match; a falsy non-boolean clears rather than flips). `app-error.test.ts` pins `onError` to JSON `{ error: 'Internal Server Error' }` (not the raw exception). `app-cors.test.ts` pins a static `allowHeaders` list (no reflect of `Access-Control-Request-Headers`) and the production origin list (`https://mase.fi` allowed, `http://localhost:5173` denied). `connection-timeouts.test.ts` pins finite pg.Pool connect/statement/query timeouts and the idle-client `pool.on('error')` listener (logs, does not throw). `day-endpoints.test.ts` drives `/today` `/yesterday` `/tomorrow` through `makeWindowFilterDb` so a shifted Helsinki `[start, end)` fails, and empty yesterday is `200 { slots: [] }`. Its clock is frozen at 00:30 Helsinki (winter and summer), when UTC is still on the previous date, and every date/window is a literal, so a handler taking "today" from UTC `toISOString()` fails (finding #9934). `routes/now-cheaper-than.test.ts` also imports `js/hero-calc.js` so the API value and the tint that reads it are asserted together. `collector.test.ts` pins CLI dispatch: missing `DATABASE_URL` and invalid `--backfill` exit 1 without opening a DB; bare argv calls `collectPrices`; `--backfill=DATE` calls `backfillPrices` with Helsinki midnight (not UTC). `utils/project-env.test.ts` pins `projectEnvPath` (API and collector entries in `src/` or `dist/` → project-root `.env`, not `backend/.env`). `collectors/backfill-arg.test.ts` pins `--backfill` / `--backfill=DATE` (date-only → Helsinki midnight, not UTC) / ISO-instant passthrough / invalid dates. `slot-validation.test.ts` and `heatmap-precision.test.ts` drop ±Infinity NUMERIC values the same way as NaN (never JSON-null). `collectors/sahkotin-validation.test.ts` pins mixed-slot drop vs all-invalid abort (empty `prices` is still end-of-history) and the backfill write path (MWh→kWh × the slot date's VAT, then `toFixed(5)`, with 23%/24%/10%/25.5% slots in one chunk as literal strings). `utils/price-conversion.test.ts` pins every `vatRateAt` change on both sides of Helsinki (not UTC) midnight and the throw before the first known rate. `utils/http.test.ts` and `collectors/slot-filter.test.ts` pin the shared `fetchUpstreamJson` (timeout, redirect follow, `<source> API error` wording) and `filterValidSlots` skip-warn. `collectors/upsert.test.ts` pins `makePriceInsert` 5-decimal canonicalization (negatives, IEEE-754 `0.1+0.2`) and `upsertPrices` ON CONFLICT (`prices.datetime` target, both `EXCLUDED` price columns). `collectors/spot-hinta-fetch.test.ts` pins the live collect URL (`TodayAndDayForward`) and `AbortSignal.timeout(FETCH_TIMEOUT_MS)`. `index.test.ts` pins `main()` graceful shutdown: missing `DATABASE_URL` throws before listen; SIGTERM waits for `server.close` then `closeDb` then `process.exit(0)`; a 10s guard still drains if close never settles. `range-window.test.ts` pins from===to on the 23h spring-forward and 25h fall-back days (and a multi-day range containing the transition), not only winter 24h.

## Test-support doubles

`backend/src/test-support/` holds the shared fakes. New route/collector tests import from there rather than hand-casting `as unknown as Db`. The module is test-only — tsconfig `exclude`s `src/test-support/**` so it stays out of `dist/` and the `tsc --noEmit` graph (like `*.test.ts`); it's verified by vitest at runtime.

**`fake-db.ts`** — WHERE-ignoring / insert-chain / heatmap-execute doubles:

- `makeSelectDb` / `makeCountingSelectDb` — select-chain resolves a fixed result set (counting variant probes cache hits)
- `makeInsertDb` / `makeCountingInsertDb` / `makeCapturingInsertDb` — insert-chain resolves a pg-style `{ rowCount }` (count chunks, or capture the rows written plus the `onConflictDoUpdate` config)
- `makeHeatmapExecuteDb` — `getHeatmap`'s two-call `db.execute` protocol (cells, then ISO week-number). Also exposes `lastCellQuery` / `lastWindow` so tests can assert the Helsinki `[Mon 00:00, next Mon 00:00)` SQL bounds (heatmap cannot use `makeWindowFilterDb` — it goes through `db.execute`, not the select-chain). Used by heatmap-week-cache, heatmap-precision, heatmap-cache, and heatmap-window tests

**`window-db.ts`** — WHERE-bound-aware doubles for tests whose contract *is* the query window (`makeSelectDb` ignores WHERE and would hide a shifted bound):

- `makeWindowKeyedDb` — looks up fixtures by the query's `gte` lower bound. Used by `now-yesterday-slot`
- `makeWindowFilterDb` — filters seeded rows by the `[gte, lt)` the handler actually queried, and records that window. Used by `range-window` and `day-endpoints`

**`fetch-stub.ts`** — `okJson` / `failedResponse` / `stubFetch` / `stubFailedFetch`

**`fake-dom.ts`** — `fakeEl` / `fakeDocument` document-like double for renderer copy tests (getElementById + createElement, no jsdom)

**`rows.ts`** — `RawRow` / `Slot` types, `row()` / `taxedRow()` (DB rows) and `slot()` / `stepSlots()` (frontend 15-min slots). Frontend tests import these rather than rebuilding `{datetime, priceWithTax, priceNoTax}` per file.
